# Dribull Chart Review Prompt

Review the rendered daily chart for the provided A-share candidate. Return strict JSON only.

The current Rust CLI can screen and chart dribull candidates, but native dribull review is not implemented. Use this prompt only if the user explicitly asks for manual/subagent chart review of dribull charts.

Use the schema in `review-rubric.md`.
