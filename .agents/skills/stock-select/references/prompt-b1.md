# B1 多模态图表复盘 Prompt

新 CLI 当前不支持 `b1 run/review` 的模型复盘，正常生产流程不要安排 b1 多模态复盘。

仅当用户明确要求人工查看 b1 图表时使用本提示。读取图表后只给人工观察意见，不要暗示 b1 有可用模型排序。

关注：

- 趋势结构与价格位置。
- 成交量是否支持继续观察。
- 前期异常上涨或回撤风险。
- MACD 阶段和日/周线节奏。

若需要写入新 CLI annotation，仍按 `review-rubric.md` 的 schema 输出，但不要把它描述为 b1 model review。

